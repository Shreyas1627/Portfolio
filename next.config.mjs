/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  output: 'export',
  basePath: '/Portfolio',
  assetPrefix: '/Portfolio',
  reactCompiler: true,
};

export default nextConfig;
